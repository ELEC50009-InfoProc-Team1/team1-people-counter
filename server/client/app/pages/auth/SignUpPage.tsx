import { Typography } from "@mui/material";
import type { ReactNode } from "react";

export const SignUpPage = (): ReactNode => {
    return (
        <Typography>ouch but sign up</Typography>
    );
};
